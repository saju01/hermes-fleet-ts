# macOS
.DS_Store

# Xcode
build/
DerivedData/
*.xcuserstate
xcuserdata/
*.xcworkspace/xcuserdata/

# Swift Package Manager
.build/
.swiftpm/
Package.resolved

# Local agent/automation scratch state (never ship)
.hermes/
.worktrees/
.schema

# Xcode generated user-facing extras
*.xcresult
xcodespeed/

# SwiftCoverage / test artifacts
*.profraw

# OS
.DS_Store

# Local environment overrides (endpoints, device IDs, credentials)
.env
.env.*
!.env.example

# Local/hosted QA forensics and operator evidence (never ship)
/hosted/
/local/
/evidence.md

# Rebuilt from the pinned official SDK; never commit binaries or node state.
/Vendor/TailscaleKit.xcframework/
